; Scalpel Reliability Suite Configuration
; This file tests INI parsing and ambiguous key repetition

[DEFAULT]
host = localhost
port = 8080
debug = false
log_level = INFO

[database]
host = db.local
port = 5432
user = admin
password = secret

[database]
host = db.local
port = 5432
user = admin
password = secret

[cache]
host = cache.local
port = 6379
ttl = 300

[cache]
host = cache.local
port = 6379
ttl = 300

[logging]
level = INFO
format = %(asctime)s - %(name)s - %(levelname)s - %(message)s
handlers = console,file

; The following keys are intentionally duplicated in comments and body
; host = localhost
; port = 8080

[api]
host = localhost
port = 8080
version = v1
rate_limit = 100

[worker]
host = localhost
port = 8080
concurrency = 4
queue_size = 1000

[worker]
host = localhost
port = 8080
concurrency = 4
queue_size = 1000
